language: python

sudo: required

cache: pip

services: postgresql

python:
  - "2.7"
  - "3.4"

install: 
  - pip install -r requirements.txt --quiet
  - python manage.py migrate --noinput

before_script:
  - createuser --createdb --no-superuser --no-createrole postgres
  - createdb -O vagrant travisci

script: 
  - flake8
  - python manage.py test